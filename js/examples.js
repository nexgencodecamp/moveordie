// index.html

/* ARRAYS */
var shoppingList = ['milk', 'eggs', 'whiskey'];

var numbers = [1, 2, 3, 4, 6, 10, 100, -1]

/* Objects */
var me = {
    name: "Pete",
    age: 25,
    hobbies: ["bass", "piano", "golf", "cooking"]
}

/* if examples */

if(onePlayerLeft == true){
    win()

    
if (levelBlock === SPRITE_WALLBLOCK_CODE) {
    Crafty.e('WallBlock')
}
else if (levelBlock === SPRITE_CORNERBLOCK_CODE) {
    Crafty.e('CornerBlock')
}
else if (levelBlock === SPRITE_PLATFORMBLOCK_CODE) {
    Crafty.e('PlatformBlock')
}
else if (levelBlock === SPRITE_FLOORBLOCK_CODE) {
    Crafty.e('FloorBlock')
}

/* if else problem */
function getPercentage(percent){
    if(percent >= 90){
        return 'A'
    }   
    else if (percent >= 80){
        return 'B'
    }
    else if (percent >= 70) {
        return 'C'
    }
}


/* for loop 1 to 10 */
for(var i=0; i<10; i++){
    console.log(i);
}

/* Events in a Game */

/* BUILD A LEVEL */

/* Crafty Components */

/* Enable Player */

/* Enable Falling Blocks */